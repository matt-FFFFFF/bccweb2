# SPDX-FileCopyrightText: 2026 British Club Challenge authors
# SPDX-License-Identifier: MPL-2.0
# COMMITTED TEMPLATE. Copy to terraform.tfvars (gitignored) and adjust:
#   cp iac/bootstrap/terraform.tfvars.example iac/bootstrap/terraform.tfvars
#
# `tfstate_storage_account_name` must be globally unique (3–24 chars,
# lowercase letters and digits). The canonical `terraform_umis` map below is
# the dev + prod + shared model: one UMI per downstream stack, each owning
# exactly its pre-created stamp or shared RG.

tfstate_storage_account_name = "stbccweb13afe"

github_environments = ["dev", "prod", "shared"]

terraform_umis = {
  dev = {
    stamp_rg   = "stamp-dev"
    github_env = "dev"
  }
  prod = {
    stamp_rg   = "stamp-prod"
    github_env = "prod"
  }
  shared = {
    github_env = "shared"
  }
}
